# Frontend Vite environment example
# Copy to frontend/.env and adjust as needed for local development

VITE_API_BASE=http://localhost:5000/api

# Notes:
# - VITE_API_BASE should point to your backend API base (including /api if your axios instance expects it).
# - Do not commit secrets in frontend env files.
